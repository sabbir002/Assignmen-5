import React from "react";

const Avatar = ({ u }) => {
  //    const {auth} =  useAuth()
  // //   const { state } = useProfile();
  //   console.log(auth?.user);
  return (
    <>
      {/* {user?.avatar ? (
        <img src={""} alt="profile image" className="rounded-full" />
      ) : (
        getFirstLetterOfName(user?.firstName)
      )} */}
    </>
  );
};

export default Avatar;
